[
  {
    "skills": ["qovery-builder-portal"],
    "query": "Generate the builder portal — we already ran qovery-builder-env",
    "files": [],
    "expected_behavior": [
      "Detects an existing builder-platform-config.yaml from qovery-builder-env (Phase 1.2)",
      "Asks for SSO config, branding (company name, primary color, logo), and per-builder limits",
      "Copies every backend file from templates/src/server/* and templates/src/shared/types.ts verbatim",
      "Generates the agent-driven frontend pages (routes, components) per the Phase 2.3 spec",
      "Uses templates/Dockerfile and templates/package.json as-is",
      "Presents the Phase 4 deployment plan and waits for confirmation before deploying"
    ]
  },
  {
    "skills": ["qovery-builder-portal"],
    "query": "We use Okta — wire up SSO",
    "files": [],
    "expected_behavior": [
      "Loads reference/phase3-configuration.md (Okta section)",
      "Sets SSO_PROVIDER=oidc, SSO_ISSUER_URL=https://{okta-domain}, SSO_CLIENT_ID, SSO_CLIENT_SECRET",
      "Provides the exact callback URL (https://{portal-domain}/auth/callback) for the user to register in Okta",
      "Stores SSO_CLIENT_SECRET and SESSION_SECRET as Qovery secrets, never as plain env vars"
    ]
  },
  {
    "skills": ["qovery-builder-portal"],
    "query": "Add a 'Data Analytics' template that points to a different blueprint env",
    "files": [],
    "expected_behavior": [
      "Reads reference/phase3-configuration.md (templates section) and reference/phase6-operations.md",
      "Adds a new entry to the `templates:` list in builder-platform-config.yaml with name, description, blueprint_env_id, and icon",
      "Commits the change to git",
      "Reminds the user to redeploy the portal so it picks up the new template",
      "Does NOT regenerate the portal code — the new template is purely a config change"
    ]
  }
]
