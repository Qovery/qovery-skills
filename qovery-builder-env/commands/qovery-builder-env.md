---
description: Set up self-service builder environments for non-tech teams using Qovery
---

Set up self-service builder environments for non-tech teams (sales, finance, operations, marketing) using Qovery.

If arguments are provided, use them as context:
- `$ARGUMENTS` — team name (e.g., "sales"), number of builders, builder experience tier (visual/vscode/terminal), or Qovery Console URL

Follow the qovery-builder-env skill to:
1. Understand who the builders are and what they need
2. Set up the platform foundation (project, RBAC, SSO)
3. Create a builder environment template with AI coding tools
4. Provision individual builder workspaces
5. Configure cost controls and lifecycle management
6. Version-control the setup (git + optional Terraform)
7. Generate onboarding guides for builders and the platform team
